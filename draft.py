import tkinter as tk
import tkinter.font as tkFont

root = tk.Tk()
print(tkFont.families())
root.destroy()
# This code is part of a Tic-Tac-Toe game that uses tkinter for the GUI. It includes a setup window for game configuration, a main menu, and utility functions for styling and centering windows. The game supports single-player and multiplayer modes, AI difficulty settings, and customizable player names and symbols.